[build]
  base    = "" 
  command = "npm install && npm run build"
  publish = "renderer/out"

[build.environment]
  # تغيير النسخة إلى 18 سيحل مشكلة الـ ignoreDeprecations فوراً
  NODE_VERSION = "18"